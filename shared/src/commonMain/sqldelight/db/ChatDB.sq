CREATE TABLE harvest_chat(
    uid TEXT NOT NULL PRIMARY KEY,
    userId TEXT,
    content TEXT,
    time INTEGER
);

insertChat:
INSERT OR REPLACE INTO harvest_chat(uid, userId, content, time) VALUES(?,?,?,?);

selectAllChats:
SELECT * FROM harvest_chat ORDER BY time DESC;

deleteAllChats:
DELETE FROM harvest_chat;